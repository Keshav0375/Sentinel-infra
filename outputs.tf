# ─────────────────────────────────────────────────────────────────────────────
# Root outputs.
#
# Intentionally empty until phase 4 task 4.4 wires the modules together. Outputs
# are the seam other repos consume (ACR login server, AKS name, Key Vault URI,
# App Service hostname), so they are defined once, at the end, against real
# module addresses rather than accumulated piecemeal.
# ─────────────────────────────────────────────────────────────────────────────
